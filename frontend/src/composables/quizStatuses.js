const quizStatuses = () => {
  const completed = 'CMP'
  const inProgress = 'INP'
  const defaultStatus = 'DEF'
  const displayAll = 'ALL'

  return { completed, inProgress, defaultStatus, displayAll }
}

export default quizStatuses