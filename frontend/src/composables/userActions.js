const userActions = () => {
  const isFollowUser = 'FF'
  const isFollowBackUser = 'FB'
  const isUnFollowUser = 'UF'

  return { isFollowUser, isFollowBackUser, isUnFollowUser }
}

export default userActions